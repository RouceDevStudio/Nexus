[setup]
pkgs = ["nodejs_22", "python311", "python311Packages.pip"]

[install]
cmds = [
  "npm install --legacy-peer-deps",
  "npm install multer sharp mammoth pdf-parse",
  "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "bash start.sh"
